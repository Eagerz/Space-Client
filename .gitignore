node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Large official Minecraft trailer MP4s (fetched via scripts/fetch-home-trailers.js)
src/assets/video/trailers/*.mp4

# Stripe payments backend secrets & player DB
backend/.env
backend/data/

# Runtime crash cases / local bot state
data/

# Expo mobile
mobile/.env
mobile/.expo/
mobile/dist/

# Cursor project agents — local only (AI-editable; not published to GitHub).
# Includes electron-auto-updater and release helpers used for the cloud update system.
.cursor/agents/
